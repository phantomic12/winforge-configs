# winforge-configs
Example config repo for phantomic12/winforge reusable workflow. Users fork this, edit their own profiles + autounattend, and call winforge from their workflows. No need to fork winforge itself.
